{
  "name": "NotTodayThankyou",
  "version": "1.0.0",
  "description": "Automatically closes PRs that fail selected tests and checks.  ",
  "main": "dist/index.js",
  "scripts": {
    "build": "pnpm tsc --outDir dist",
    "bundle": "pnpm esbuild src/index.ts --bundle --platform=node --target=node20 --outfile=dist/index.js",
    "test": "echo \"The tests are run in Github Actions - the main entry point is ./github/workflows/test_matrix.yml\" && exit 1"
  },
  "keywords": [],
  "author": "James Parrott",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^26.2.0",
    "esbuild": "^0.28.2",
    "typescript": "^7.0.2"
  },
  "dependencies": {
    "@actions/core": "^3.0.1",
    "@actions/github": "^9.1.1"
  },
  "pnpm.onlyBuiltDependencies": ["esbuild"]
}
